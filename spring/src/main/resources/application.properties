## Database Properties
spring.datasource.driver-class-name = com.mysql.cj.jdbc.Driver

spring.datasource.url =jdbc:mysql://localhost\:3306/crud?useTimezone=true&serverTimezone=UTC
spring.datasource.username=root


##spring.datasource.url =jdbc:mysql://localhost\:3306/crud_spring?useTimezone=true&serverTimezone=UTC
##spring.datasource.username =root
## spring.datasource.password =admin


## Hibernate Properties
# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5InnoDBDialect

# Hibernate ddl auto (create, create-drop, validate, update)

## para o banco continuar ativo mesmo se algo for alterado no código
# spring.jpa.hibernate.ddl-auto = update
## sempre que algo for mudado no projeto, ele já destroi o que já tinha no bd e cria um novo
spring.jpa.hibernate.ddl-auto = create-drop